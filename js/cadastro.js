// Array global para armazenar necessidades
let necessidades = [];

// Esboço do evento de submit do formulário
document.getElementById('formNecessidade').addEventListener('submit', (e) => {
  e.preventDefault();
  // Lógica de cadastro será implementada
});